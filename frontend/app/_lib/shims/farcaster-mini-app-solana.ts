// Privy treats this package as an optional peer dependency.
// This app does not use the Farcaster mini app Solana integration, so a
// no-op shim keeps the optional dynamic import resolvable across bundlers.
export {};
